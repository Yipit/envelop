-r requirements.txt
coverage==3.6
mock==1.0.1
nose==1.3.0
steadymark==0.4.5
sure==1.2.2

# They don't know how to handle version numbers; We used to have it but now
# it's breaking cause they excluded it from pypi. (0.3.3)
rednose
